## 目标
- 在 `/Users/michael/Documents/Code/Trae/Tarot2/conversation_temp.md` 写入一份完整交接文档，让后端 IDE 的大模型通过阅读即可了解项目全貌、当前状态与下一步改造联调方案。

## 文档结构
- 项目概览：双仓结构、前后端职责、密钥安全原则
- 前端现状：生产可用、静态资源与域绑定、Hedge 并发配置、端到端调用期望
- 后端现状：Gemini 端点 400（模型格式）、Zhipu 端点 404、CORS 配置与域名
- 后端改造任务：两个端点的上游 URL、请求体、Header、超时与错误返回、CORS 白名单与安全
- 部署与联调：后端 Redeploy、前端 Production 验证步骤、只读连通性测试方式
- 回滚与兜底：常见错误与一键修复（Promote→Redeploy→Refresh）、模型与版本兼容策略
- 验证清单：生产域端到端出结果的判定标准、失败回退文案

## 交付
- 生成上述内容到 `conversation_temp.md`；你拷贝到后端仓库后，新的 IDE 会话可直接用文档开展工作。

## 后续
- 你确认后，我立即写入文档并回传摘要，然后继续执行后端只读连通性与前端端到端验证的自动化步骤（只读）。