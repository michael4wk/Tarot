## 发现与提醒
- 后端已声明完成两个端点与 CORS，符合前端约定：
  - Gemini：v1beta `models/{model}:generateContent?key=...`，body 透传 `contents/parts/text`
  - Zhipu：`/api/ai/zhipu`，Bearer 密钥，`model/messages` 体
  - CORS：动态白名单、仅放行 POST 与 `Content-Type`
- 重要提醒：浏览器 CORS 预检会走 `OPTIONS`；若服务端仅处理 POST 而不应答 `OPTIONS`，前端会被拦截。建议在两个端点统一实现 `OPTIONS` 预检响应（204 + CORS 头）。

## 验收与联调步骤
1) 后端：添加/确认 `OPTIONS` 预检支持（两个端点通用）
- 响应：`204 No Content`
- 头：`Access-Control-Allow-Origin: <匹配的Origin>`、`Access-Control-Allow-Methods: POST, OPTIONS`、`Access-Control-Allow-Headers: Content-Type`、`Vary: Origin`、`Access-Control-Max-Age: 600`
- 保持 POST 路由逻辑不变；仅补足预检路径。

2) 后端：在 Vercel 后端项目点击一次 `Redeploy`
- 确保新代码生效；无需前端改动。

3) 我来执行只读验证（不需你操作）
- 对两个端点发起只读连通性检查：`POST /api/ai/gemini/generate` 与 `POST /api/ai/zhipu`（体同前端）
- 记录状态码与片段；若 2xx 且返回 JSON，则通过；若 4xx/5xx，定位 URL/版本/体/密钥/CORS 问题。

4) 你在生产主域做端到端验证（最少动作）
- 打开 `https://tarotpro.vercel.app`：输入问题 → 抽牌 → 选牌 → 开始解读
- 预期：Network 出现 `POST https://tarotpro-backend.vercel.app/api/ai/...`；页面展示结构化 `core/actions/warnings`
- 若异常：我将给出最少操作（通常为 Promote → Redeploy → Domains Refresh + 无痕强刷）

## 回滚与兜底
- 若 Gemini 仍报 400：确认 v1beta 路径与 body 未顶层传 `model`；必要时后端实现 v1/v1beta双尝试
- 若 Zhipu 仍 404：检查路由命名与文件位置是否落到 `/api/ai/zhipu`
- 若 CORS 失败：检查是否已正确应答 `OPTIONS` 与动态白名单
- 若主域资源异常：对当前生产部署执行 `Promote → Redeploy`；Domains 对自定义域 `Refresh`；随后无痕强刷

## 触发点与分工
- 现在请你通知后端补充 `OPTIONS` 预检并 Redeploy；完成后告知我
- 我再执行只读连通性与生产端到端验证；通过后进入上线回归
